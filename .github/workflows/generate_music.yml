name: Gerar Música

on:
  workflow_dispatch:
    inputs:
      prompt:
        description: 'Prompt em português'
        required: false
        default: 'breakcore caótico'
      style:
        description: 'Estilo (vazio = detectar do prompt)'
        required: false
        default: ''
      duration:
        description: 'Duração em segundos'
        required: false
        default: '30'
      seed:
        description: 'Seed (vazio = aleatório)'
        required: false
        default: ''
      audition:
        description: 'Rodar audição dos SoundFonts antes (treino dos bancos)'
        required: false
        default: 'false'

permissions:
  contents: read

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Cache de dependências Python
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
        restore-keys: ${{ runner.os }}-pip-

    - name: Cache da memória da IA (audição dos bancos + feedback + modelos)
      uses: actions/cache@v4
      with:
        path: |
          memory
          models
        # chave única por run (cache só salva em miss) + restore pelo prefixo
        key: ${{ runner.os }}-ia-memory-v1-${{ github.run_id }}
        restore-keys: |
          ${{ runner.os }}-ia-memory-v1

    - name: Instalar dependências
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Instalar FluidSynth + SoundFont GM
      continue-on-error: true
      run: |
        sudo apt-get update -qq
        sudo apt-get install -y -qq fluidsynth fluid-soundfont-gm

    - name: Sanidade da integração
      run: |
        python -c "from midi_composer import MidiComposer; print('MidiComposer OK')"
        python -c "import real_instruments as ri; print(len(ri.INSTRUMENTS), 'instrumentos')"
        python -c "import music_generator as mg; print(len(mg._sf2_files()), 'bancos .sf2 em soundfonts/')"

    - name: Audição dos SoundFonts (treino dos bancos)
      if: github.event.inputs.audition == 'true'
      run: python music_generator.py --audition

    - name: Gerar música
      env:
        PROMPT: ${{ github.event.inputs.prompt }}
        STYLE: ${{ github.event.inputs.style }}
        DURATION: ${{ github.event.inputs.duration }}
        SEED: ${{ github.event.inputs.seed }}
      run: |
        ARGS=(--duration "${DURATION:-30}")
        [ -n "$PROMPT" ] && ARGS+=(--prompt "$PROMPT")
        [ -n "$STYLE" ] && ARGS+=(--style "$STYLE")
        [ -n "$SEED" ]  && ARGS+=(--seed "$SEED")
        python music_generator.py "${ARGS[@]}"

    - name: Listar arquivos gerados (debug)
      if: always()
      run: find song_output -type f | sort

    - name: Upload dos arquivos gerados
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: musica-run-${{ github.run_number }}
        path: |
          song_output/**/*.mid
          song_output/**/*.wav
          song_output/**/*.json
        if-no-files-found: error
        retention-days: 30
